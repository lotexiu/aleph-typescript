export const buildPrAnalysis = ({
	submodule,
	branch,
	changedFiles,
	codeChanges,
	distChanges,
	packageDiff,
	readmeDiff,
	api,
	apiExtractorCurrent,
	apiExtractorReference,
	apiExtractorDiff,
	commits,
	release,
	openIssues,
	issues,
	buildTag,
	impact,
	type,
}) => ({
	package: submodule.shortName,
	timestamp: new Date().toISOString(),
	baseBranch: branch,
	tags: {
		impact,
		type,
		build: buildTag,
	},
	comparison: {
		changedFiles,
		codeChanges,
		distChanges,
		packageJson: packageDiff,
		readme: readmeDiff,
		api: {
			levitate: api,
			apiExtractor: {
				current: apiExtractorCurrent,
				reference: apiExtractorReference,
				reportDiff: apiExtractorDiff,
			},
		},
	},
	commits,
	release,
	openIssues,
	issues,
});
